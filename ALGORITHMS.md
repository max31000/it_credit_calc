# Алгоритмы расчёта (v2)

Спецификация финансового движка [`src/lib/engine.ts`](src/lib/engine.ts).
Дизайн переработки: [`docs/specs/2026-08-12-calculator-v2-design.md`](docs/specs/2026-08-12-calculator-v2-design.md).
Тесты-инварианты: [`src/lib/__tests__/engine.test.ts`](src/lib/__tests__/engine.test.ts).

## 1. Базовые формулы

- Сумма кредита: `P = стоимость − первоначальный взнос`
- Месячная ставка: `r = годовая / 12 / 100`
- Аннуитет: `PMT = P × r / (1 − (1+r)^(−n))`, где `n` — оставшийся срок в месяцах
- Рыночная ставка после слёта: `ключевая − дисконт банка + 1.5` (% годовых)

## 2. Симулятор стратегии

Один проход по месяцам `t = 1..горизонт`, состояние: `debt`, `savings`, `rate`, `pmt`.

Каждый месяц (пока долг > 0 и срок не вышел):

1. `interest = debt × rate`; обязательный платёж `mandatory = min(pmt, debt + interest)`.
2. Платёж:
   - **гасить досрочно (prepay)**: `payment = min(max(mandatory, freeMonthly), debt + interest)` —
     весь бюджет уходит в кредит; аннуитет пересчитывается каждый месяц на остаток срока
     (досрочка с уменьшением платежа);
   - **копить (save)**: `payment = mandatory`, аннуитет фиксированный.
3. `debt −= payment − interest`.
4. Остаток бюджета `freeMonthly − payment` инвестируется: `savings = savings × (1 + r_dep) + invest`.
5. После полного погашения долга **обе** стратегии инвестируют весь `freeMonthly`
   (важно: иначе сравнение искусственно занижает результат досрочного погашения).

Обязательный платёж вносится всегда, даже если `freeMonthly` меньше него.

### Слёт (конец месяца `slipMonth`)

1. `rate` становится рыночной.
2. Стратегия save (по умолчанию) вносит все накопления в долг:
   `paid = min(savings, debt)`; `debt −= paid`; `savings −= paid`.
3. Аннуитет пересчитывается: `pmt = PMT(debt, r_market, n − slipMonth)`.

Слёт позже горизонта или после конца срока не влияет на расчёт.

### Налоговые вычеты (конец каждого года, если задана зарплата)

Считаются отдельно для каждой стратегии (у них разные проценты):

1. Налоговая база года = `12 × зарплата`.
2. Сначала расходуется остаток **имущественной** базы: лимит `min(2 000 000, цена)`.
3. Затем **процентная** база: фактически уплаченные проценты (пул с переносом между
   годами), суммарный лимит базы — `3 000 000 ₽`.
4. Возврат = `НДФЛ(база) − НДФЛ(база − имущественная − процентная)` по прогрессивной
   шкале 2025: 13% до 2.4М, 15% до 5М, 18% до 20М, 20% до 50М, 22% свыше.
   Возврат автоматически ограничен уплаченным НДФЛ; неиспользованные базы переносятся.
5. Возврат реинвестируется: у save — в накопления, у prepay — в досрочное погашение
   (после погашения долга — в накопления).

## 3. Варианты расчёта в `calculate()`

| Вариант | Условие | Использование |
|---|---|---|
| prepay без слёта | всегда | базовая линия |
| save без слёта | всегда | payoffMonth, slipAnalysis, инвестдоход, база для slipLoss |
| prepay со слётом | slipMonth > 0 | отображаемая линия |
| save со слётом + внесение | slipMonth > 0 | отображаемая линия, slipLoss |
| save со слётом без внесения | slipMonth > 0 | dumpBenefit |

Отображаемый сценарий (`series`, `summary`) — со слётом, если он задан.

## 4. Производные метрики

- **payoffMonth** — первый месяц, когда `savings ≥ debt` (базовый save): накоплений
  хватает закрыть ипотеку целиком.
- **slipAnalysis[t]** — если слёт случится в месяц `t` (базовый save):
  `paymentWithoutPrepay = PMT(debt(t), r_market, n − t)`;
  `paymentWithPrepay = PMT(max(0, debt(t) − savings(t)), r_market, n − t)`.
- **safetyMonth** — первый `t`, где `paymentWithPrepay ≤ льготный PMT`: слёт больше
  не поднимает платёж выше льготного.
- **slipLoss** = `netWorth(save без слёта) − netWorth(save со слётом + внесение)` на горизонте.
- **dumpBenefit** = `netWorth(слёт + внесение) − netWorth(слёт без внесения)`;
  может быть отрицательным, если доходность вклада выше рыночной ставки.
- **advantageSave** = `netWorth(save) − netWorth(prepay)` в отображаемом сценарии.

## 5. Контрольные значения

Параметры: квартира 7М, взнос 1.5М (кредит 5.5М), ставка 6%, срок 20 лет,
бюджет 100к/мес, вклад 16%, горизонт 10 лет, слёт в месяц 36, ключевая 16%, дисконт 0.5%.

| Величина | Значение |
|---|---|
| PMT | ≈ 39 404 ₽ |
| Рыночная ставка | 17% |
| Долг save на 36-м месяце (без слёта) | ≈ 5 031 775 ₽ |
| Накопления save на 36-м месяце | ≈ 2 775 100 ₽ |
| Долг prepay на 36-м месяце | ≈ 2 648 000 ₽ |
| Платёж при слёте без внесения | ≈ 75 551 ₽ |
| Платёж при слёте с внесением | ≈ 33 890 ₽ |

## 6. Допущения

- Ставки и доходность постоянны; номинальные рубли, без инфляции.
- Досрочка уменьшает платёж (не срок).
- Вычеты начисляются раз в год и сразу реинвестируются.
- Доход по вкладу без НДФЛ на проценты.
